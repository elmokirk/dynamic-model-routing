{
  "name": "cc-dynamic-model-routing",
  "version": "0.3.0",
  "description": "Dynamic Model Routing plugin for Claude Code",
  "type": "module",
  "bin": { "dmr": "./dist/cli.js" },
  "scripts": {
    "build": "tsx esbuild.config.ts",
    "test": "vitest run",
    "bench": "tsx bench/benchmark.ts",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
