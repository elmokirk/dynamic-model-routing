---
description: Route a prompt through DMR to get the best model/effort recommendation
argument-hint: "<your prompt>"
---

# /dmr run

Scores `$ARGUMENTS` against routing rules and recommends a model + effort level.
Respects current mode (confirm/auto/off) from `.claude/dmr-session.json`.

## Usage
/dmr run implement user authentication with JWT
